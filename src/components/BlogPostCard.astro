---
import Card from './Card.astro'

interface Props {
  title: string
  description: string
  url: string
}

const { title, description, url } = Astro.props
---

<Card title={title} description={description} href={url} hoverEffect class="w-full block" />
